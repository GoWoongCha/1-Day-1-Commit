Chapter07_01. 튜플

#학습목표
1. 튜플의 특징
2. 튜플 만들기
3. 튜플을 리스트로 만들기
4. 패킹과 언패킹
5. 튜플함수

#튜플의 특징
1. 시퀀스 자료형
2. 수정, 추가, 삭제가 불가능한 리스트
3. 메모리 사용이 효율적
4. 읽기만 가능하기 때문에 데이터 손실 염려가 없다.

#튜플 만들기
튜플=(데이터, 데이터, 데이터)
튜플=데이터, 데이터, 데이터

a = (3, 4, 5)
a = "패스트캠퍼스", 3, True

a = (30,)
a = 30,
-> 튜플을 하나만 만들 때, 괄호가 없으면 int 30으로 인식한다. 따라서, 뒤에 ','를 찍어줘야한다.

#튜플을 리스트로 만들기
1. a = tuple([5, 6, 7])
2. x = list(range(10))
   a = tuple(x)
3. x = 5,6,7
   a = list(x)

#패킹과 언패킹
numbers = 3,4,5 #패킹
a,b,c = numbers #언패킹

numbers = [3,4,5]
a,b,c = numbers

패킹: 여러 개의 데이터를 하나의 변수에 할당하는 것
언패킹: 컬렉션의 각 데이터를 각각의 변수에 할당하는 것

#튜플 함수
a = 10, 20, 30, 40, 30

1. 특정값의 인덱스 구하기 a.index(20)
2. 특정값의 개수	a.count(30)
3. 최대값, 최소값	max(a), min(a)
4. 합계	sum(a)