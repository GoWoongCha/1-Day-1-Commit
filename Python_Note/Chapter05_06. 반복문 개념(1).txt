Chapter05_06. 반복문 기본개념(1)

#학습목표
1. 반복문을 사용하는 이유 - 반복적인 작업을 코드로 해결하기 위해서
2. 시퀀스 자료형 - 순서가 있는 자료형, 리스트, 문자열, rage 객체, 튜플, 딕셔너리
3. for문 사용법 - for 변수 in 시퀀스 자료:
				명령문

for a in [1, 2, 3, 4]
	print(a)

4. range 명령어 - range(10) - 0~9까지 숫자를 포함하는 range 객체를 만들어준다.